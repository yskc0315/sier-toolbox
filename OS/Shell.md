# シェル

## シェルとは

ユーザーとOSの橋渡しをするプログラム。

今回はGit Bashを利用した。

---

## source

source ファイル名

現在のシェルでスクリプトを実行する。

---

## なぜsourceが必要か

通常

./script.sh

で実行すると子プロセスで実行される。

子プロセスで変更した環境変数は親プロセスへ反映されない。

一方

source script.sh

は現在のシェル自身を書き換えるため、

PATHなどの環境変数を変更できる。

仮想環境を有効化するときはsourceが必要。