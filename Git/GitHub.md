# GitとGitHub

## git remote

リモートリポジトリを登録する。

例

git remote add origin git@github.com:ユーザー名/リポジトリ名.git

originは慣例的な名前。

---

## git remote -v

登録されているリモートリポジトリを確認する。

(fetch)

取得用URL

(push)

送信用URL

通常は同じURLになる。

---

## Gitの流れ

作業

↓

git add

↓

git commit

↓

git push

GitHubへ反映